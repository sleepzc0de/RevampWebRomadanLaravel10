includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 4

    paths:
        - app

    # Baseline menampung error legacy yang ada saat adopsi; kode BARU tetap
    # diperiksa. Kecilkan baseline secara bertahap seiring perbaikan.
